import React, { FC } from "react";
import { Button } from "antd";
const Home: FC = () => {
	return (
		<div>
			<Button type="primary">Hello world</Button>
		</div>
	);
};
export default Home;
